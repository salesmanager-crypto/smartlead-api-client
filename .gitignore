.env
node_modules/
.DS_Store

# runtime state / logs — machine-local, and some contain real business data
.claude/settings.local.json
.last-checkpoint
scripts/.deliverability-state.json
scripts/*.log
scripts/*-log.txt
scripts/deliverability-log.txt
logs/
dist/

# credentials — never commit (this repository is public)
client_secret*.json
*credentials*.json
service-account*.json
token*.json
*.pem
*.p12

# built dashboard, contains live lead data
dashboard/_site/

# plaintext copies of encrypted dashboard data and the built artifact (person-level data)
.private/
