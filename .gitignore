.env
node_modules/

# runtime state / logs — machine-local, and some contain real business data
.claude/settings.local.json
.last-checkpoint
scripts/.deliverability-state.json
scripts/*.log
scripts/deliverability-log.txt
logs/

# CSV exports contain real lead PII — never commit
exports/
*.csv

# Google OAuth credentials/tokens — never commit
.google-token.json
client_secret*.json
